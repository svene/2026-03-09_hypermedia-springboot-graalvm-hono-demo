<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.3</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>

	<groupId>org.svenehrke.demo</groupId>
	<artifactId>hda-springboot-graalvm-hono-demo</artifactId>
	<version>1.0-SNAPSHOT</version>
	<name>2026-03-09_hda-springboot-graalvm-hono-demo</name>
	<description>2026-03-09_hda-springboot-graalvm-hono-demo</description>

	<properties>
		<java.version>21</java.version>
		<!-- Encoding for sources, resources, Javadoc -->
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
	</properties>

	<build>
		<plugins>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.15.0</version> <!-- latest stable -->
				<configuration>
					<release>21</release>
				</configuration>
			</plugin>

			<!-- Unit tests, runs during the 'test' phase: *Test.java, *Tests.java, *TestCase.java -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
			</plugin>

			<!-- Integration tests, runs during the 'integration-test' phases: *IT.java, *ITCase.java -->
			<!-- Failsafe runs after the application is packaged, so you can test the “real thing” (e.g., start a Spring Boot app, hit REST endpoints) -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<configuration>
					<release>${java.version}</release>
					<encoding>${project.build.sourceEncoding}</encoding>
				</configuration>
			</plugin>

		<plugin>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-maven-plugin</artifactId>
		</plugin>

			<plugin>
				<groupId>cz.habarta.typescript-generator</groupId>
				<artifactId>typescript-generator-maven-plugin</artifactId>
				<version>4.1.1</version>
				<executions>
					<execution>
						<id>generate</id>
						<goals>
							<goal>generate</goal>
						</goals>
						<phase>process-classes</phase>
					</execution>
				</executions>
				<configuration>
					<jsonLibrary>jackson2</jsonLibrary>
					<classPatterns>
						<pattern>org.svenehrke.demo.inbound.**.*Model</pattern>
						<pattern>org.svenehrke.demo.inbound.**.*VM</pattern>
					</classPatterns>
					<classes>
						<class>org.svenehrke.demo.inbound.web.JTSPersonRouteName</class>
						<class>org.svenehrke.demo.inbound.web.JTSPersonEventName</class>
					</classes>
					<outputFile>${project.basedir}/src/main/java/org/svenehrke/demo/inbound/web/generated/types/vm-types.d.ts</outputFile>
					<outputFileType>declarationFile</outputFileType>
					<outputKind>module</outputKind>
					<mapClasses>asInterfaces</mapClasses>
					<mapEnum>asUnion</mapEnum>
				</configuration>
			</plugin>

			<plugin>
				<!--
				  typescript-generator (above) can only emit *types*, but the TS side needs the
				  mutation endpoint path templates as runtime string values. This inline Groovy
				  script reflects over HonoWebApiSharedConsts.HonoWebApiConsts (the Java source of
				  truth, also used in PersonActionController's @PutMapping/@DeleteMapping) and writes
				  generated/types/web-api-consts.ts. Runs in process-classes, same as typescript-generator.
				-->
				<groupId>org.codehaus.gmavenplus</groupId>
				<artifactId>gmavenplus-plugin</artifactId>
				<version>5.1.0</version>
				<executions>
					<execution>
						<id>generate-web-api-consts-ts</id>
						<phase>process-classes</phase>
						<goals>
							<goal>execute</goal>
						</goals>
						<configuration>
							<scripts>
								<script><![CDATA[
									import java.lang.reflect.Modifier

									def quote = { s -> '"' + s.toString().replace('\\', '\\\\').replace('"', '\\"') + '"' }

									def cls = Class.forName('org.svenehrke.demo.inbound.web.HonoWebApiSharedConsts$HonoWebApiConsts')

									def entries = cls.declaredFields
										.findAll { it.type == String && Modifier.isStatic(it.modifiers) }
										.sort { it.name }
										.collect { f -> "\t${f.name}: ${quote(f.get(null))}," }
										.join('\n')

									def out = new File(project.basedir,
										'src/main/java/org/svenehrke/demo/inbound/web/generated/types/web-api-consts.ts')
									out.parentFile.mkdirs()
									out.text = """\
// GENERATED from HonoWebApiSharedConsts.java by the gmavenplus script in pom.xml - do not edit.
export const HonoWebApiConsts = {
${entries}
} as const;
"""
									log.info("wrote ${out}")
								]]></script>
							</scripts>
						</configuration>
					</execution>
				</executions>
				<dependencies>
					<dependency>
						<groupId>org.apache.groovy</groupId>
						<artifactId>groovy</artifactId>
						<version>5.1.1</version>
						<scope>runtime</scope>
					</dependency>
				</dependencies>
			</plugin>

	</plugins>

	</build>

	<dependencies>
		<!-- Example: JUnit 5 for testing -->
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>

		<!-- Support for IntelliJ autocomplete of configuration properties: -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-configuration-processor</artifactId>
			<optional>true</optional>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.graalvm.polyglot</groupId>
			<artifactId>polyglot</artifactId>
			<version>25.0.2</version>
			<scope>compile</scope>
		</dependency>
		<dependency>
			<groupId>org.graalvm.polyglot</groupId>
			<artifactId>js</artifactId>
			<version>25.0.2</version>
			<type>pom</type>
			<scope>compile</scope>
		</dependency>

		<!-- Database -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-jdbc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.hsqldb</groupId>
			<artifactId>hsqldb</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-hsqldb</artifactId>
		</dependency>
		<dependency>
			<groupId>net.datafaker</groupId>
			<artifactId>datafaker</artifactId>
			<version>2.5.2</version>
		</dependency>

	</dependencies>

</project>
